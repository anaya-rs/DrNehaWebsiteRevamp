# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Production builds
**/dist/
build/

# Environment variables — never commit real credentials
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
server/.env

# User-uploaded media — served from local disk in dev, S3 in production.
# Run `git rm -r --cached server/uploads/` if these were previously committed.
server/uploads/

# Claude Code internal directory
.claude

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs/
*.log

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage
coverage/

# Temporary folders
tmp/
temp/
